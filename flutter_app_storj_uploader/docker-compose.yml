services:
  storj_uploader_flutter:
    build: .
    container_name: storj_uploader_flutter_app
    ports:
      - "3000:80"
    networks:
      - storj_uploader_frontend_container_app_storj-network
    environment:
      - FLUTTER_WEB_USE_SKIA=true
    restart: unless-stopped

  # Backend service - using external backend on host
  # backend:
  #   image: storj_uploader_frontend_container_app-backend
  #   container_name: storj_uploader_backend_api_container_app
  #   ports:
  #     - "8010:8010"
  #   networks:
  #     - storj-network
  #   restart: unless-stopped

networks:
  storj_uploader_frontend_container_app_storj-network:
    external: true